Server application build for Windows.
- Uses the IP address of machine (type ipconfig into command prompt to find your IP).
- Uses Port 9999.
- May require that you allow the application through the device's firewall.